#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include "_putchar.c"
#include <string.h>
#include <stdarg.h>

int _putchar(char c);
int _puts(const char *);
int _printf(const char *format, ...);

#endif
